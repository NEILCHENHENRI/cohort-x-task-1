# Dev-time only — used by optimize_gepa.py for GEPA's Claude reflection model.
# Copy to .env (gitignored) and fill in your key. NEVER committed.
# The offline inference path (predict_ollama.py) must never read this.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
