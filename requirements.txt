# CohortX Task 1 — Requirements
# Install: pip install -r requirements.txt
#
# Note: do NOT upgrade torch, torchvision, or numpy in a Colab environment —
# Colab manages these and upgrading breaks the runtime.

# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.26.0
sentence-transformers>=2.6.0

# ── Data / utils ──────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
joblib>=1.3.0
tqdm>=4.66.0
lxml>=4.9.0

# ── Evaluation metrics ────────────────────────────────────────────────────────
word2number>=1.1
spacy>=3.7.0
nltk>=3.8.0
# After installing spacy: python -m spacy download en_core_web_sm
# After installing nltk:  python -c "import nltk; nltk.download('wordnet'); nltk.download('wordnet_ic'); nltk.download('omw-1.4')"

# ── Inference backends ────────────────────────────────────────────────────────
ollama>=0.3.0          # local inference via Ollama server (THE submission path)
openai>=1.0.0          # GPT-4o-mini inference
anthropic>=0.25.0      # Claude inference

# ── Optional / GPU-only (NOT needed for the offline submission) ────────────────
# bitsandbytes is CUDA-only and FAILS to install on Apple Silicon (Mac M-series).
# It is only used by the 8-bit alt backend local_llm/predict_qwen.py on an NVIDIA
# GPU. Leave commented on Mac; uncomment only on a CUDA machine.
# bitsandbytes>=0.43.0   # 4-bit/8-bit quantization for large models on GPU

# ── GEPA prompt optimization (DEV-TIME ONLY — not on the offline inference path) ─
dspy>=3.2.0            # GEPA optimizer + DSPy program/metric
python-dotenv>=1.0.0   # load ANTHROPIC_API_KEY from .env (optimize_gepa.py)
kagglehub>=0.3.0       # download the competition dataset (download_data.py)
